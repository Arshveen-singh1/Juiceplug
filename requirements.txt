# Core
torch>=2.1
transformers>=4.44
peft>=0.11
accelerate>=0.30

# Quantization (skip on Apple Silicon / CPU-only — see README)
bitsandbytes>=0.43

# Tools
duckduckgo-search>=6.1

# Misc
pydantic>=2.5

# Dev / test (optional — see requirements-dev.txt)
